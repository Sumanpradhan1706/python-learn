friends =["Apple", "Banana", "Mango", "Orange"]

print(friends[0])  # Accessing the first element
friends[0]= "Tometo" # unlike strings, lists are mutable, so we can change the value at index 0

print(friends[0])  # Output the modified list
